writer.writerow([])

writer.writerow(["SUMMARY"])

writer.writerow([
    "TOTAL_TASKS",
    len(dag.tasks)
])

writer.writerow([
    "ROOT_TASKS",
    ",".join(root_tasks)
])

writer.writerow([
    "END_TASKS",
    ",".join(end_tasks)
])

writer.writerow([
    "ESTIMATED_FULL_DAG_RUNTIME_HOURS",
    round(total_runtime, 2)
])